suite: test mailder
#templates:
tests:
  - it: should render
    asserts:
      - template: deployment.yaml
        matchSnapshot:
          path: spec
      - template: deployment.yaml
        isKind:
          of: Deployment
      - template: deployment.yaml
        hasDocuments:
          count: 1
