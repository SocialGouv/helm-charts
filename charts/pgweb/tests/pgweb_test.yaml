suite: test mailder
#templates:
tests:
  - it: should render
    asserts:
      - template: "deployment.yaml"
        matchSnapshot:
          path: spec
      - template: "ingress.yaml"
        matchSnapshot:
          path: spec
      - template: "service.yaml"
        matchSnapshot:
          path: spec
      - template: "hpa.yaml"
        matchSnapshot:
          path: spec
      - template: "serviceaccount.yaml"
        matchSnapshot:
          path: spec
